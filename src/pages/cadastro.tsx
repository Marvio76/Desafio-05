import React from 'react';
import LoginComponent from '../components/login';
const Cadastro = () => {
    return (
        <div>
            <LoginComponent />
        </div>
    );
};

export default Cadastro;
